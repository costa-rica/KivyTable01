This app builds kivy table. It’s features:
-scrolls
-jump to scroll positions (beginning, middle, end)
-sort by columns (ascending, descending)
-call api to get data into table
-delete rows
-Toggle between all data and 20 observations
-Buttons act like toggles


See gif for preview of the app.
